package com.collections.list;

import java.util.ArrayList;


///Iterator interface provides the facility of iterating the elements in forward direction only.
// in list, need to shift elements after every manipulation

// In arraylist, start capacity is 10, and increment is >>1 +old value
// copy is an generic method, arrays are created internally

// Custom logic for equals if (!(o1==null ? o2==null : o1.equals(o2))) return false;
// hashcode basically adds up all the hash codes after 31*hashcode


public class List {
	
	
//	Collection<E>
	
//	List<E>
//	ArrayList<E>

}
